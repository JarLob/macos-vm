name: Sample SSH server workflow

on:
  workflow_dispatch:
    inputs:
      os:
        description: 'OS'
        required: true
        default: 'windows-latest'
        type: choice
        options:
          - windows-latest
          - windows-2019
          - macos-latest
          - ubuntu-latest
          - ubuntu-24.04-arm
          - ubuntu-22.04-arm
      timeout:
        description: 'How long to run (minutes)'
        required: false
        type: number
        default: "10"

jobs:
  sample:
    runs-on: ${{ inputs.os }}
    steps:
    - uses: actions/checkout@v4
    - name: Start SSH server
      timeout-minutes: ${{ fromJSON(inputs.timeout) }}
      uses: jarlob/ssh-server-action@latest
      with:
        ngrok-authtoken: "${{ secrets.NGROK_AUTHTOKEN }}"
        ssh-public-key: "${{ secrets.SSH_PUBLIC_KEY }}"
